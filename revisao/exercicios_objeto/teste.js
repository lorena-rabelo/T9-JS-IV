class Signo {
  constructor(nome,elemento, regente) {
    this.nome = nome;
    this.elemento = elemento;
    this.regente = regente;
  }
}

const lorena = new Signo('touro', 'terra', 'plutão');


console.log (lorena)